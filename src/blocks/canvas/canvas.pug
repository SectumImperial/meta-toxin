mixin canvas(options = {})
  -
    const {
      canvasClass = '',
      canvas = {}
    } = options
  div(
    class = `canvas ${canvasClass}`
    data-canvas = canvas
  ) 
    .canvas__chart
    .canvas__legend