const CHART = 'js-canvas__chart';
const LEGEND = 'js-canvas__legend';

const SVG = 'canvas__svg';
const ITEM = 'canvas__item';
const UNIT = 'canvas__unit';

const SVG_CLASS = 'js-canvas__svg';
const ITEM_CLASS = 'js-canvas__item';
const UNIT_CLASS = 'js-canvas__unit';

export {
  CHART,
  LEGEND,
  SVG,
  ITEM,
  UNIT,
  SVG_CLASS,
  ITEM_CLASS,
  UNIT_CLASS,
};
