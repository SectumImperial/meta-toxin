/* eslint-disable linebreak-style */
const DAYS_COMPUTE = 'js-order-counting__days_compute';
const DAYS_COMPUTED = 'js-order-counting__days_computed';
const FEE_COMPUTE = 'js-order-counting__fee_compute';
const FEE_COMPUTED = 'js-order-counting__fee_computed';
const ADD_FEE_COMPUTE = 'js-order-counting__add-fee_compute';
const ADD_FEE_COMPUTED = 'js-order-counting__add-fee_computed';
const SUM = 'js-order-counting__sum';

export {
  DAYS_COMPUTE,
  DAYS_COMPUTED,
  FEE_COMPUTE,
  FEE_COMPUTED,
  ADD_FEE_COMPUTE,
  ADD_FEE_COMPUTED,
  SUM,
};
