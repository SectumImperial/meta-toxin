mixin input(options = {})
  - if(options.datepicker) 
    - var datepicker = 'datepicker'
  - if(options.dropdown) 
    - var dropdown = 'input__dropdown'
  input(
    type = options.type
    name = options.name
    id = options.id
    class = `input ${options.classInput} ${datepicker || ''} ${dropdown || ''}`
    value = options.value 
    placeholder = options.placeholder
    datePicker = options.datepicker 
  )  
  