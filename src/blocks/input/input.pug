mixin input(options = {})
  - 
    const {
      type = 'text',
      name = '',
      id = '',
      value = '',
      placeholder = '',
      readonly = false,
      pattern = '.*',
      arrow = false,
      isRequired = false,
      isDate = false,
      isHovered = false,
      isAutocomplete = false,
    } = options

    const arrows = {
      expandMore: 'icon__expand_more',
      forward: 'icon__arrow_forward input_arrow-forward',
    }

    const dateClass = isDate ? '_dateInput' : ''
    const hoveredClass = isHovered ? 'input__field_hovered' : ''

  .input
    input.input__field(
      type = type
      name = name
      id = id
      class = `${dateClass} ${hoveredClass}`
      value = value 
      placeholder = placeholder
      readonly = readonly
      pattern = pattern
      required = isRequired
      autocomplete = isAutocomplete ? "on" : "off"
    )  
    - if(arrow && arrows[arrow])
      div(
        class = `input_arrow ${arrows[arrow]}` 
      )