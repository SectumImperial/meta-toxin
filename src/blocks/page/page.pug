include ../header/header.pug
include ../landing/landing.pug

body.page
  +header({classHeader: 'page__header container',
          href: '#'})
  +landing({class: 'page__main'})
  main.main
  footer.page__footer.footer