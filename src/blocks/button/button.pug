mixin button(options = {}) 
  - 
    const {
      btnType = 'button',
      type = 'submit',
      isArrow = false,
      btnColor = 'white',
      btnWrapperColor = 'violet',
      btnSize = 'small',
      href = 'error-page.html',
      content = '',
      isHovered = false
    } = options 

    const colorBtn = {
      white: 'white',
      violet: 'violet',
    } 

    const wrapperColor = {
      transparent: 'button_transparent',
      violet: 'button_violet',
    }

    const sizeBtn = {
      tiny: 'button_tiny',
      small: 'button_small',
      medium: 'button_medium',
      big: 'button_big',
      large: 'button_large'
    }

    const hoverClass = isHovered ? 'button_hovered' : ''

  .button(
    class = `${wrapperColor[btnWrapperColor]} ${hoverClass}`
  )
    - if(btnType === 'button')
      button(
        class = `button__button button__button_${colorBtn[btnColor]} ${sizeBtn[btnSize]}`
        type = type
        )
          span.button__content= content
      - if (isArrow) 
        .button__icon
    - if(btnType === 'link')
      a(
        class = `button__button button__button_link button__button_${colorBtn[btnColor]} ${sizeBtn[btnSize]}`
        href = href
        )
          span.button__content= content


