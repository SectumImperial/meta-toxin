include ../logo/logo.pug
include ../nav/nav.pug
include ../button/button.pug

mixin header(options = {})
  -
    const {
      isShadow = false,
      isLogged = false,
      navigation = {},
    } = options


  header.header(
    class = {
      header_shadow: isShadow
    }
    )
    .header__content
      .header__logo
        +logo({
          href: 'landing-page.html'
        })
      .header__icon.js-header__icon
        span
      .header__nav-wrapper.js-header__nav-wrapper
        .header__nav 
          +nav(navigation)
        if !options.isLogged
          .header__button
            +button({
              content: 'Войти', 
              color: 'white',
              size: 'small',
              href: 'login-page.html'
              })
          .header__button
            +button({
              color: 'violet',
              content: 'Зарегистрироваться',
              size: 'small',
              href: 'registration-page.html'
              })
        else
          p.header__user Юлий Цезарь
